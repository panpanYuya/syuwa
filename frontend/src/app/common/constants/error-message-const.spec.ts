import { ErrorMessageConst } from './error-message-const';

describe('ErrorMessageConst', () => {
  it('should create an instance', () => {
    expect(new ErrorMessageConst()).toBeTruthy();
  });
});
